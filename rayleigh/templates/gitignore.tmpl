# results/ is a working folder, not its own repo. If a parent repo tracks it, keep the
# machinery and bulky run data out; the shipped write-up (.docx) + figures/ are the deliverable.
rayleigh.yaml
designdocs/experiments.yaml
data/
archive/
__pycache__/
*.pyc
